services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./database.sqlite:/app/database.sqlite
      - ./uploads:/app/uploads
    environment:
      - NODE_ENV=production
      - JWT_SECRET=change_this_in_production
    restart: always
